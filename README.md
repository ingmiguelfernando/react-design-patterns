# react-design-patterns
 some interesting patterns to be applied on react js.

 # Layout Components
 React components that deal primarily with arranging other components on the page. The idea of Layout components is: "Our components shouldn't know where they're being displayed"
